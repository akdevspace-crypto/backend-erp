import { z } from 'zod'; const booleanish = z.preprocess(v => v, z.boolean()); const schema = z.object({ roleId: z.string().min(1), loginEnabled: booleanish, forceLogout: booleanish.optional(), email: z.any().optional(), password: z.any().optional() }); try { schema.parse({ roleId: 'Employee', loginEnabled: true, email: 'test@example.com' }); console.log('success'); } catch(e) { console.log(e.issues); }
